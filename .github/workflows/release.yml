name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: glmocr-windows
            binary_path: dist/glmocr
            archive_ext: zip
          - os: ubuntu-latest
            artifact_name: glmocr-linux
            binary_path: dist/glmocr
            archive_ext: tar.gz
          - os: macos-latest
            artifact_name: glmocr-macos
            binary_path: dist/glmocr
            archive_ext: tar.gz

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Create venv (Unix)
        if: matrix.os != 'windows-latest'
        run: uv venv .venv
        shell: bash

      - name: Create venv (Windows)
        if: matrix.os == 'windows-latest'
        run: uv venv .venv
        shell: pwsh

      - name: Install CPU-only torch (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          uv pip install --python .venv/bin/python \
            torch torchvision \
            --index-url https://download.pytorch.org/whl/cpu
        shell: bash

      - name: Install CPU-only torch (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          uv pip install --python .venv\Scripts\python.exe `
            torch torchvision `
            --index-url https://download.pytorch.org/whl/cpu
        shell: pwsh

      - name: Install selfhosted deps + pyinstaller (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          uv pip install --python .venv/bin/python \
            -e ".[selfhosted]" pyinstaller
        shell: bash

      - name: Install selfhosted deps + pyinstaller (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          uv pip install --python .venv\Scripts\python.exe `
            -e ".[selfhosted]" pyinstaller
        shell: pwsh

      - name: Cache PP-DocLayoutV3 model
        id: cache-layout-model
        uses: actions/cache@v4
        with:
          path: models/PP-DocLayoutV3
          key: pp-doclayoutv3-safetensors-v1

      - name: Download PP-DocLayoutV3 model (Unix)
        if: matrix.os != 'windows-latest' && steps.cache-layout-model.outputs.cache-hit != 'true'
        run: |
          .venv/bin/python -c "
          from huggingface_hub import snapshot_download
          snapshot_download('PaddlePaddle/PP-DocLayoutV3_safetensors', local_dir='models/PP-DocLayoutV3', local_dir_use_symlinks=False, ignore_patterns=['*.msgpack','*.h5','flax_model*','tf_model*','rust_model*'])
          "
        shell: bash

      - name: Download PP-DocLayoutV3 model (Windows)
        if: matrix.os == 'windows-latest' && steps.cache-layout-model.outputs.cache-hit != 'true'
        run: |
          .venv\Scripts\python.exe -c "
          from huggingface_hub import snapshot_download
          snapshot_download('PaddlePaddle/PP-DocLayoutV3_safetensors', local_dir='models/PP-DocLayoutV3', local_dir_use_symlinks=False, ignore_patterns=['*.msgpack','*.h5','flax_model*','tf_model*','rust_model*'])
          "
        shell: pwsh

      - name: Cache GLM-OCR model
        id: cache-ocr-model
        uses: actions/cache@v4
        with:
          path: models/GLM-OCR
          key: glm-ocr-zai-org-v1

      - name: Download GLM-OCR model (Unix)
        if: matrix.os != 'windows-latest' && steps.cache-ocr-model.outputs.cache-hit != 'true'
        run: |
          .venv/bin/python -c "
          from huggingface_hub import snapshot_download
          snapshot_download('zai-org/GLM-OCR', local_dir='models/GLM-OCR', local_dir_use_symlinks=False, ignore_patterns=['*.msgpack','*.h5','flax_model*','tf_model*','rust_model*','original/*'])
          "
        shell: bash

      - name: Download GLM-OCR model (Windows)
        if: matrix.os == 'windows-latest' && steps.cache-ocr-model.outputs.cache-hit != 'true'
        run: |
          .venv\Scripts\python.exe -c "
          from huggingface_hub import snapshot_download
          snapshot_download('zai-org/GLM-OCR', local_dir='models/GLM-OCR', local_dir_use_symlinks=False, ignore_patterns=['*.msgpack','*.h5','flax_model*','tf_model*','rust_model*','original/*'])
          "
        shell: pwsh

      - name: Build binary (Unix)
        if: matrix.os != 'windows-latest'
        run: .venv/bin/pyinstaller glmocr.spec
        shell: bash

      - name: Build binary (Windows)
        if: matrix.os == 'windows-latest'
        run: .venv\Scripts\pyinstaller.exe glmocr.spec
        shell: pwsh

      - name: Smoke test binary (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          ./dist/glmocr/glmocr --help
          ./dist/glmocr/glmocr --help | grep -v -i "api.key\|maas\|zhipu\|cloud" && echo "PASS: no cloud refs in help"
        shell: bash

      - name: Smoke test binary (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          .\dist\glmocr\glmocr.exe --help
          $help = .\dist\glmocr\glmocr.exe --help 2>&1 | Out-String
          if ($help -match "api.key|maas|zhipu|cloud") { Write-Error "FAIL: cloud refs in help" } else { Write-Host "PASS: no cloud refs in help" }
        shell: pwsh

      - name: Archive Linux/macOS binary
        if: matrix.archive_ext == 'tar.gz'
        run: tar -czf ${{ matrix.artifact_name }}.tar.gz -C dist glmocr
        shell: bash

      - name: Archive Windows binary
        if: matrix.archive_ext == 'zip'
        run: Compress-Archive -Path dist\glmocr -DestinationPath ${{ matrix.artifact_name }}.zip
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            ${{ matrix.artifact_name }}.zip
            ${{ matrix.artifact_name }}.tar.gz
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            glmocr-windows.zip
            glmocr-linux.tar.gz
            glmocr-macos.tar.gz
          generate_release_notes: true
